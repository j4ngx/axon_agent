rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Access via Admin SDK (used by Axon bot) bypasses these rules.
      // Deny all public and client-side access.
      allow read, write: if false;
    }
  }
}
