rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Axon uses the Firebase Admin SDK (which bypasses rules), so
      // all client-side access is denied.
      allow read, write: if false;
    }
  }
}
